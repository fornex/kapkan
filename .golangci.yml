version: "2"

linters:
  default: standard
  enable:
    - misspell
    - unconvert
    - copyloopvar
  settings:
    staticcheck:
      checks: ["all", "-ST1000", "-QF1008"]

formatters:
  enable:
    - gofmt
    - goimports
