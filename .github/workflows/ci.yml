name: CI

on:
  push:
    branches: ["**"]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    name: build & test (race)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      - name: Build
        run: go build ./...
      - name: Test (race)
        run: make test

  lint:
    name: golangci-lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          # The golangci-lint action manages its own module cache.
          cache: false
      - uses: golangci/golangci-lint-action@v7
        with:
          # Tracks the latest v2.x (matches .golangci.yml version "2").
          # Pin to a specific tag for fully reproducible runs.
          version: latest

  govulncheck:
    name: govulncheck (report-only)
    runs-on: ubuntu-latest
    # Report-only: a reachable advisory in the upstream gobgp BGP speaker
    # (GO-2026-4736) has no fixed release yet, so govulncheck cannot pass while
    # gobgp is a dependency. It still runs on every PR and surfaces findings in
    # the logs. Flip continue-on-error to false once that advisory is fixed.
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      - name: Run govulncheck
        run: go run golang.org/x/vuln/cmd/govulncheck@latest ./...
