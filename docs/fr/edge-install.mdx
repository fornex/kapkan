export const metadata = {
  title: "Installer un nœud edge",
  description:
    "Pas à pas : déclarer les zones et le nœud sur le cerveau, préparer nginx ou Angie, installer l'unité kapkan edge, la vérifier, et passer en production.",
};

# Installer un nœud edge

Ce guide mène une machine d'un paquet nginx (ou Angie) standard à un [nœud edge](/docs/edge) géré,
servant vos zones avec des certificats émis et des décisions par requête. Lisez d'abord la page de
concepts si les mots *zone*, *service de décision* ou *séparation rapide/lent* sont nouveaux pour
vous.

Il vous faut : un cerveau (le démon `kapkan`) joignable depuis la machine en HTTPS, un token `agent`
sur celui-ci, une machine Linux avec nginx ≥ 1.22 ou Angie installé, les ports TCP 80 et 443
ouverts sur le monde — et UDP 443 aussi, sur la machine, dans le groupe de sécurité et dans l'ACL
en amont s'il y en a une, si une zone doit parler [HTTP/3](/docs/edge#http3-quic) —, et un DNS pour
les zones pointant vers la machine (ou vers l'anycast/la VIP que les machines partagent).

## 1. Sur le cerveau : zones et nœuds

Écrivez le fichier de zones — ce sont des **données de locataire**, tenues à l'écart de
`kapkan.yaml` — et pointez le cerveau vers lui :

```yaml
# /etc/kapkan/zones.yaml
zones:
  - name: shop.example.com
    origins: ["10.0.0.10:8080"]
    policy: { mode: decide, failure_mode: open, rate: { rps: 50, concurrency: 20 } }
  - name: static.example.com
    origins: ["10.0.0.20:80"]
    policy: { mode: none }             # proxy only, no decisions
```

```yaml
# /etc/kapkan/config.yaml (the brain)
edge:
  zones_file: /etc/kapkan/zones.yaml   # absolute; the whole reload fails on a broken file
  nodes:
    - name: edge-1                     # must equal the node's controller.name
    - name: edge-2
  stale_after_seconds: 15              # a node unheard-from this long counts as lost
api:
  tokens:
    - { name: edge-1, token_env: KAPKAN_EDGE_1_TOKEN, role: agent, node: edge-1 }
    - { name: edge-2, token_env: KAPKAN_EDGE_2_TOKEN, role: agent, node: edge-2 }
```

**Cadrez les zones sur les nœuds** lorsque la flotte a des PoP : donnez à chaque nœud les
hostgroups dont il sert les zones (`edge.nodes[].hostgroups: [edge-us]`, avec le littéral `global`
pour les zones valables sur toute la flotte) et à chaque zone son `hostgroup` ; un nœud sans périmètre
ne sert que les zones globales, une zone sans hostgroup est globale. La règle, le littéral `global`
et le mode opératoire sont dans [Placer les zones sur les
nœuds](/docs/edge#placer-les-zones-sur-les-nœuds) ; le plafond de certificats en double se compte
par placement, pas par flotte (voir [Limites](/docs/edge#limites-à-connaître)). Cadrer un nœud,
quel qu'il soit, impose que chaque token agent soit lié, comme ci-dessus.

`kapkan -check-config /etc/kapkan/config.yaml` valide les deux fichiers (il suit
`edge.zones_file`), et `kapkan -dump-zones-schema` imprime le schéma JSON du fichier de zones pour
votre éditeur. Donnez à chaque nœud **son propre token `agent`, lié à lui avec `node`** : un token
lié est refusé sous l'identité de tout autre nœud avant même que le cerveau ne fasse quoi que ce
soit de ce nom, de sorte qu'un token volé sur une machine ne peut plus parler pour aucun autre nœud
— sa présence, ses rapports et ses créneaux ACME restent les siens — et seul ce token-là doit être
renouvelé. Un fichier de zones qui ne peut être lu ou
validé lors d'un rechargement **garde les zones précédentes en service** — le cerveau ne sert
jamais un document à moitié cassé. Chaque clé est dans la [référence des zones](/docs/zones).

<Callout type="info" title="Le token agent est un identifiant qui émet des certificats — liez-le">
Un nœud edge s'authentifie avec un token `agent` (voir [authentification](/docs/authentication)).
Outre l'interrogation du document, il utilise ce token pour coordonner l'émission des certificats
via le cerveau — un token **non lié** peut donc agir sous l'identité de n'importe quel nœud, et le
renouveler après une compromission revient à renouveler celui de toute la flotte. Liez chaque token
à son nœud (`api.tokens[].node`) ; tant que chaque token agent n'est pas lié, `-check-config`, le
journal du démon et l'`unbound_agent_tokens` de l'inventaire le disent. Ce que la liaison apporte,
c'est l'usurpation d'identité et le rayon de rotation ; ce qui limite l'ensemble des zones pour
lesquelles un token lié volé peut émettre, c'est le [périmètre de
placement](/docs/edge#placer-les-zones-sur-les-nœuds) du nœud : agissant sous l'identité de son
propre nœud, il ne peut prendre le créneau d'émission et publier une autorisation de clé ACME que
pour les zones que ce périmètre couvre (toutes les zones sur une flotte sans périmètres) — traitez la
compromission d'un nœud comme une exposition de certificat pour les zones de ce nœud. Pour migrer
une flotte depuis un unique token partagé : ajoutez un token lié par nœud **avec son propre
secret** (une valeur partagée avec l'entrée non liée est ambiguë et refusée par un `401` pour chaque
nœud qui la présente), basculez le fichier d'environnement de chaque nœud et regardez le
`last_token` de l'inventaire basculer, puis supprimez l'entrée partagée en dernier — un token non
lié continue de fonctionner entre-temps. Gardez le token dans le fichier d'environnement du nœud,
jamais dans `edge.yaml`.
</Callout>

## 2. Sur la machine : nginx ou Angie

Kapkan possède un répertoire d'inclusion et pilote `nginx -t` et les rechargements ; il ne fournit
ni ne supervise nginx. Faites inclure la génération en service par votre configuration principale
**une seule fois**, à l'intérieur de `http {}` :

```nginx
http {
    # ... your defaults ...
    include /var/lib/kapkan-edge/conf/live/*.conf;
}
```

Retirez (ou prévoyez de garder, voir ci-dessous) tout `default_server` sur `:80`/`:443` — Kapkan
génère son propre fourre-tout qui refuse les hôtes inconnus. Le site par défaut de Debian en
déclare un ; désactivez ce site ou définissez `omit_catch_all: true` dans `edge.yaml`. Confirmez
ensuite que l'inclusion compile avant que quoi que ce soit ne soit généré (une inclusion à joker
d'un répertoire qui n'existe pas encore convient à nginx) :

```sh
nginx -t
```

`live` lui-même est un **lien symbolique** que le nœud crée et recible à chaque génération — ne le
créez pas à la main : un répertoire à sa place bloque chaque installation (`apply: rename … live`
dans le journal) jusqu'à ce qu'il soit supprimé.

Trouvez le **groupe du worker** — `nginx` pour les paquets nginx.org et l'image Docker, `www-data`
pour le paquet propre à Debian, `angie` pour Angie. Les sockets de décision et de journal sont
créés en `0660` pour ce groupe.

**Pour HTTP/3**, le build doit porter le module et l'adresse doit accepter l'UDP :

```sh
nginx -V 2>&1 | grep -o -- --with-http_v3_module     # Angie: angie -V
```

Le nginx 1.22 de Debian 12 n'en a aucun (les zones qui demandent h3 y sont servies en TCP, et le
nœud le dit) ; le nginx 1.26.3 de Debian 13, les paquets de nginx.org à partir de 1.25 et Angie en
ont un. Ouvrez UDP 443 partout où TCP 443 est ouvert, donnez aux flux UDP un délai conntrack sur
tout pare-feu à état présent sur le chemin, et gardez la MTU de chemin à 1280 octets ou plus —
QUIC ne fragmente jamais, une diversion GRE/IPIP avec une MTU plus petite casse donc h3 alors que
TCP survit grâce au
[clamping du MSS](/docs/network-integration#diversion-hors-chemin-via-gre-ou-ipip). Si votre
`nginx.conf` fixe `ssl_protocols` au niveau `http`, il doit inclure `TLSv1.3` — QUIC ne parle rien
d'autre (les écouteurs QUIC de Kapkan le disent d'eux-mêmes). `kapkan edge -check` affiche la
disponibilité HTTP/3 du nœud dès que l'`edge.yaml` est en place.

## 3. Le nœud : `edge.yaml`, le token, l'unité

Installez le même binaire `kapkan` que sur le cerveau, puis la configuration propre au nœud :

```yaml
# /etc/kapkan/edge.yaml
dry_run: true                          # watch-only until the counts look right
controller:
  url: https://kapkan.example.net:8443 # the brain's API base, https outside a lab
  token_env: KAPKAN_EDGE_TOKEN         # the agent token's environment variable
  name: edge-1                         # must equal an edge.nodes[] entry on the brain
  report_interval_seconds: 10
state_dir: /var/lib/kapkan-edge        # cached document, generations, keys, certificates
sockets_dir: /run/kapkan-edge          # the four unix sockets nginx talks to
socket_group: nginx                    # the terminator's worker group
terminator:
  binary: nginx                        # or angie
  main_conf: /etc/nginx/nginx.conf     # passed as -c to `nginx -t` and `-s reload`
  reload: exec                         # exec | signal | command
  pid_file: /run/nginx.pid             # liveness check; required for reload: signal
acme:
  directory: ""                        # Let's Encrypt production when empty
  fallback: ""                         # optional second CA after repeated failures
  contact: ["mailto:ops@example.net"]
status_listen: 127.0.0.1:9102          # /healthz + /metrics; keep it private
```

```sh
install -m 600 /dev/null /etc/kapkan/edge.env
echo 'KAPKAN_EDGE_TOKEN=...the agent token...' > /etc/kapkan/edge.env
kapkan edge -config /etc/kapkan/edge.yaml -check
```

`-check` valide le fichier et ce qu'il nomme sur cette machine : le groupe de socket doit exister
et toute clé External Account Binding doit être bien formée (des problèmes), tandis qu'un token, un
binaire ou un `main_conf` absent n'est qu'un avertissement — la vérification peut s'exécuter avant
que le fichier d'environnement ou le terminateur soient en place. Quand le binaire est dans le
`PATH`, elle affiche aussi ce que dit `nginx -V` — type, version, cœur nginx, bibliothèque TLS, si
le build a le module HTTP/3 — et la disponibilité HTTP/3 du nœud ; un build dont le cœur nginx
tombe dans la plage d'un avis de sécurité QUIC publié reçoit un avertissement nommant la CVE
(vérifiez le changelog de votre distribution : les rétroportages ne bougent pas la version).
Mettez `quic.h3: off` dans l'`edge.yaml` pour garder HTTP/3 désactivé sur ce nœud ; `-check`
qualifie alors la disponibilité de `node_off` et cesse de répéter l'avis, tandis que le rapport du
nœud continue de le porter comme un fait sur le build.

Installez l'unité livrée, `deploy/kapkan-edge.service`, et démarrez-la :

```sh
install -m 644 deploy/kapkan-edge.service /etc/systemd/system/
systemctl daemon-reload && systemctl enable --now kapkan-edge
journalctl -u kapkan-edge -f
```

L'unité tourne en **root**, comme certbot et tout gestionnaire de configuration qui pilote nginx :
`nginx -t` ouvre les chemins de journal et de pid propres à nginx, et `nginx -s reload` signale un
master root. Elle est durcie avec `ProtectSystem=strict` et des chemins d'écriture explicites
(`/var/lib/kapkan-edge`, `/run`, et le répertoire de journaux de nginx ou d'Angie — les chemins
préfixés par `-` sont ignorés s'ils sont absents, de sorte qu'une seule unité sert l'un ou l'autre
terminateur), et son répertoire d'état est en `0711` pour que le worker puisse traverser jusqu'à la
racine `try_files` vide tandis que les clés ACME restent dans leurs propres répertoires en `0700`.
Les répertoires du rôle lui appartiennent — jamais les `/var/lib/kapkan` et `/run/kapkan` du
cerveau — de sorte qu'un cerveau et un edge peuvent partager un hôte.

<Callout type="warning" title="L'ordre de démarrage n'a pas d'importance, mais les sockets si">
Le nœud ouvre ses quatre sockets unix **en premier**, de sorte que nginx peut déjà tourner et les
sonder ; il sonde ensuite le terminateur, récupère une génération qu'un prédécesseur planté a
laissée non testée, génère le dernier document qu'il avait sur disque, et interroge seulement alors
le cerveau. Un composant qui ne peut pas démarrer — un socket qu'un autre processus sert déjà, un
groupe qui n'existe pas — termine le processus avec cette erreur et systemd le redémarre.
</Callout>

## 4. Vérifier

- `journalctl -u kapkan-edge` affiche `kapkan edge running`, puis `configuration installed` avec le
  numéro de génération et le nombre de zones.
- `curl -s 127.0.0.1:9102/healthz` répond `200` avec `"healthy":true`, `"converged":true` et
  l'ETag généré ; `ls /var/lib/kapkan-edge/conf/live/` liste `kapkan_00_common.conf` et un
  `kapkan_zone_<name>.conf` par zone.
- L'inventaire du cerveau, `GET /api/v1/edge/nodes`, liste le nœud vivant avec son rapport, et la
  vue **Edge** de la console apparaît dans la navigation dès qu'un nœud est configuré.
- Certificats : la première commande a besoin que le port 80 soit joignable depuis la CA pour
  chaque nom de zone. Guettez `certificate issued` dans le journal, puis
  `ls /var/lib/kapkan-edge/certs/<zone>/current/`. Tant qu'une zone n'a pas de certificat, son
  écouteur `:80` répond `503` à tout sauf aux défis ACME — Kapkan ne fait jamais le proxy d'une
  zone en clair.
- Requêtes : avec `dry_run: true`, un client au-dessus du débit de la zone atteint tout de même
  l'origine, avec `X-Kapkan-Mark: would-deny:rate`. Surveillez
  `kapkan_edge_decisions_total{result="would_deny"}` sur `/metrics` et les journaux de votre
  origine.
- Le palier : donnez à une zone `policy.challenge: auto` (son `challenge_options.dry_run` vaut
  `true` par défaut, donc personne n'est encore mis au défi) et lisez **qui serait mis au défi** —
  `GET /api/v1/edge/zones/status` sur le cerveau, ou la vue Edge de la console — pendant qu'un défi
  qui aurait eu lieu atteint l'origine marqué `X-Kapkan-Mark: would-challenge:table:flood`. Placez
  vos vérifications de santé, vos clients d'API et vos webhooks sous
  `challenge_options.exempt_paths` avant que le palier ne passe en production.
- HTTP/3 : le `terminator.h3.state` de l'inventaire vaut `ready` sur les nœuds où vous l'attendez ;
  avec une zone en `tls.h3: true`, `terminator.h3.serving` la nomme et `listening` vaut `true`, et
  `curl --http3-only https://<zone>/` depuis l'extérieur répond `200` — puis
  `kapkan_edge_requests_total{protocol="h3"}` commence à grimper dès qu'`advertise` est actif. Un
  nœud en `no_module`, `node_off` ou `unknown` liste la zone sous `unsupported` et la sert en TCP.

## 5. Mise en production

Définissez `dry_run: false` dans `edge.yaml` et redémarrez l'unité. Dès lors, un client au-dessus
de son plafond reçoit `429` avec `Retry-After: 1`, et une source que les rollups ont promue en
refus reçoit `403`. Resserrez le `policy.rate` d'une zone sur le cerveau quand vous êtes attaqué —
ce changement atteint le service de décision à l'interrogation suivante et **ne recharge jamais
nginx**.

Le palier passe en production par zone, dans le fichier de zones : `challenge_options.dry_run: false`
une fois que l'ensemble des candidats nomme les bons clients. Dès lors, une source qui inonde une
zone `auto` reçoit [la page de laissez-passer](/docs/edge#la-page-de-laissez-passer) avant d'être
bloquée, et une zone sous attaque peut être mise sous défi immédiatement — `policy.challenge: manual`
dans le fichier, ou, pour une durée bornée et sans modifier le fichier,
[le levier](/docs/edge#le-levier) :

```sh
curl -X POST -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' \
  https://kapkan.example.net:8443/api/v1/edge/zones/shop.example.com/challenge \
  -d '{"mode":"manual","ttl_seconds":1800,"reason":"login flood"}'
```

Pour mettre les zones en service une à la fois, `policy.dry_run: true` garde une zone en
observation seule sur chaque nœud pendant que ses voisines appliquent.

## Plusieurs nœuds et une adresse partagée

Faites pointer le `controller.name` de chaque nœud vers sa propre entrée `edge.nodes[]` et placez
les mêmes zones devant tous — chaque nœud derrière une même adresse partagée doit servir la zone,
c'est-à-dire lister son hostgroup dans `edge.nodes[].hostgroups` (ou laisser la zone globale sur des
nœuds sans périmètre). Sous anycast ou une VIP partagée, la validation HTTP-01 de la CA peut atterrir
sur n'importe quel nœud derrière cette adresse : le nœud qui commande publie sa réponse au défi
auprès du cerveau, qui la diffuse dans le document de **chaque nœud qui sert la zone**, et chacun
d'eux y répond ; un nœud sur lequel la zone n'est pas placée refuse le Host et ne peut pas
répondre. L'émission est sérialisée
par zone via le créneau du cerveau, pour qu'une flotte ne consomme pas le plafond de certificats en
double d'une CA en un après-midi, et le renouvellement est décalé aléatoirement (jitter) par nœud.
Tout cela est consultatif : sans cerveau, chaque nœud renouvelle encore de lui-même.

## Dépannage

| Symptôme | Cause et correction |
| --- | --- |
| `nginx -t` échoue avec *a duplicate default server* | Votre `nginx.conf` déclare déjà un `default_server` sur `:80` ou `:443`. Définissez `omit_catch_all: true` ou retirez le vôtre. |
| `[crit] stat() ... failed` dans le journal d'erreurs de nginx à chaque requête | Le worker ne peut pas traverser jusqu'à `state_dir/empty`. Le `StateDirectoryMode=0711` de l'unité s'en charge ; un `state_dir` créé à la main a besoin d'un `chmod 711`. |
| `connect() to unix:/run/kapkan-edge/edge-decide.sock failed (13: Permission denied)` | `socket_group` n'est pas le groupe du worker. Corrigez-le et redémarrez l'unité. |
| `healthy: false`, `generation: 0` | Aucune génération testée, propre au nœud, n'est en service : le tout premier candidat a échoué à `nginx -t` (lisez `test_error`), le générateur a refusé le document (lisez `last_error`), une génération récupérée a échoué — ou le nœud n'a encore jamais reçu de document (`brain_seen` absent ; lisez les erreurs d'interrogation du journal : `controller.url`, le token, `controller.name`). |
| `apply: rename … live: file exists` (ou `is a directory`) à chaque application | Un répertoire se trouve là où va le lien symbolique `live` du nœud — créé à la main, ou par une ancienne étape d'installation. Déplacez son contenu ailleurs et faites `rmdir` ; la tentative suivante installe. |
| `converged: false` avec `test_error` | Le dernier document a été refusé ; le précédent sert. Corrigez la cause (une origine qui ne se résout pas, une directive dans `extra_directives_file`) et le nœud réessaie de lui-même. |
| La zone reste sur `:80` en `503` | Pas encore de certificat. Vérifiez la joignabilité du port 80 depuis Internet pour ce nom, `kapkan_edge_acme_attempts_total`, et la ligne `certificate order failed` du journal. |
| Nœud affiché perdu sur le cerveau alors qu'il tourne | L'interrogation est le signal de vivacité et une interrogation retenue par le cerveau compte comme vivante ; un nœud affiché perdu a donc réellement cessé de joindre le cerveau. Vérifiez `controller.url`, le token (`401`/`403` dans le journal), que `controller.name` est déclaré sous `edge.nodes[]` (`404`), et que le nœud ne partage pas un token avec plus de quatre autres (`429`). |
| Des marques `would-challenge`, mais personne n'est jamais mis au défi | L'une des trois couches d'observation seule est active : le `dry_run` du nœud (`edge.yaml`), le `policy.dry_run` de la zone, ou le `challenge_options.dry_run` propre au palier — `true` par défaut. `GET /api/v1/edge/zones/status` nomme les nœuds sur lesquels une zone est en observation seule et indique si un défi global de zone n'est qu'un aperçu (`dry_run: true`). |
| Chaque requête vers une zone reçoit la page de laissez-passer | La zone est sous défi : `policy.challenge: manual` dans le fichier, un levier (`override` dans `GET /api/v1/edge/zones/status`), ou le déclencheur global de la zone (`challenge_active` avec `reason: zone-rps`). Un client qui la franchit n'est plus sollicité pendant `cookie_ttl_seconds` ; les clients qui ne peuvent pas conserver de cookies la voient à chaque fois. |
| Les clients voient `503` (ou passent sans défi) sur une zone dont le palier est actif | nginx n'a pas pu joindre la page de laissez-passer et a suivi `failure_mode`. Cherchez `connect() to unix:/run/kapkan-edge/edge-clearance.sock failed` dans le journal d'erreurs de nginx : le socket est en `0660` pour `socket_group`, comme le socket de décision, et le nœud journalise `clearance page listening` quand elle est prête. |
| `nginx -t` échoue avec *duplicate listen options for 0.0.0.0:443* | Votre `nginx.conf` (avec `omit_catch_all: true`) a déjà un `listen 443 quic` avec des options de socket (`reuseport`, `rcvbuf`, …) et l'ancre QUIC de Kapkan porte son propre `reuseport`. Mettez `quic.omit_anchor: true` et donnez vous-même à votre serveur `reuseport` et `ssl_protocols TLSv1.3`. |
| Une zone en `tls.h3: true` n'est servie qu'en TCP sur un nœud | Le `terminator.h3.state` de ce nœud n'est pas `ready` : `no_module` (le build n'a pas `--with-http_v3_module`), `node_off` (`quic.h3: off`) ou `unknown` (la sonde a échoué — lisez le journal). La zone est listée sous `terminator.h3.unsupported` ; les autres nœuds la servent en h3. |
| Les navigateurs sont lents à charger une zone après `h3: true` | UDP 443 est bloqué en amont alors qu'`Alt-Svc` annonce h3 : chaque navigateur met la tentative QUIC en course et attend son délai avant de retomber. Corrigez le pare-feu, ou mettez `tls.h3_options.advertise: false` en attendant ; `terminator.h3.listening` ne vous dit que la moitié côté machine. |
| `curl --http3-only` échoue alors que TCP fonctionne, avec un tunnel ou un overlay sur le chemin | MTU de chemin sous 1280 octets : QUIC ne fragmente jamais. Augmentez la MTU ou gardez la zone sur TCP (le clamping du MSS sauve TCP, pas QUIC). |
| Chaque handshake QUIC échoue alors que `nginx -t` passe et qu'`Alt-Svc` est envoyé | Un `ssl_protocols` au niveau `http` dans votre `nginx.conf` sans `TLSv1.3`, sous `omit_catch_all` avec `quic.omit_anchor: true` (le catch-all et l'ancre de Kapkan épinglent `TLSv1.3` ; votre serveur par défaut doit le faire aussi). Cherchez `SSL_do_handshake() failed … unsupported protocol` dans le journal d'erreurs de nginx. |

## Voir aussi

- [Nœuds edge](/docs/edge) — le fonctionnement et ce qui casse, et comment
- [Référence des zones](/docs/zones)
- [`kapkan edge`](/docs/cli#edge) — les options et chaque clé d'`edge.yaml`
- [Le canal edge](/docs/api#le-canal-edge)
