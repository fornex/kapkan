export const metadata = {
  title: "CLI reference",
  description:
    "Every command-line flag of the kapkan binary — config path, log format and level, the pid file and nginx-style `-s` process control, and the schema/validation utility subcommands.",
};

# CLI reference

Kapkan is a single binary. Everything it does is selected by command-line flags at startup —
there are no subcommands beyond the utility modes below. All logs go to **stderr**; the REST
API and Prometheus metrics are served over HTTP on `api.listen` (default `127.0.0.1:8080`), while
the utility flags (`-version`, `-dump-schema`) print their output to **stdout**.

```sh
kapkan [flags]
```

## Flags

| Flag | Default | Description |
| --- | --- | --- |
| `-config <path>` | `configs/dev.yaml` | Path to the YAML [configuration file](/docs/configuration). |
| `-log-format <fmt>` | `json` | Log output format: `json` (structured, for log collectors) or `text` (human-readable, for local runs). Any value other than `text` is treated as `json`. |
| `-log-level <lvl>` | `info` | Minimum log level: `debug`, `info`, `warn`, `error`. An unrecognized value falls back to `info`. |
| `-check-config <path>` | — | Validate the config file at `<path>` and exit — does **not** start the daemon. See [below](#validating-a-config). |
| `-dump-schema` | `false` | Print the configuration JSON schema to stdout and exit. See [below](#dumping-the-schema). |
| `-version` | `false` | Print the build version and exit. The same version is exposed at runtime via `/api/v1/status` and the `kapkan_build_info` [metric](/docs/metrics). |
| `-check-update` | `false` | Run the opt-in [update check](/docs/configuration) once and exit — `0` up to date, `10` an update is available, `1` on error. Polls the GitHub Releases API; sends only the request itself, never node identity or config. |
| `-pid-file <path>` | `/run/kapkan/kapkan.pid` | Where the daemon writes its process id on start (removed on clean shutdown); read by `-s` to locate the running daemon. A write failure is non-fatal — the daemon still runs, only `-s` is unavailable. |
| `-s <signal>` | — | Signal a running daemon and exit, by reading `-pid-file`: `reload`, `stop` or `quit`. Does **not** start a daemon. See [below](#controlling-a-running-daemon). |

<Callout type="info" title="json or text?">
Use `-log-format text` for local development — it is the format the
[Quickstart](/docs/quickstart) uses. Keep the default `json` in production so a log
collector can parse the structured fields; the [systemd unit](/docs/deployment) ships with
`-log-format json -log-level info`.
</Callout>

## Running the daemon

With no utility flag, Kapkan opens its flow listeners, REST API and (when not in dry-run) BGP
speaker, then runs until it receives `SIGINT` or `SIGTERM`, at which point it shuts down
cleanly. It exits non-zero only on a fatal startup or runtime error.

```sh
# Local, human-readable logs against the bundled dev config:
kapkan -config configs/dev.yaml -log-format text

# Production-style: structured logs at info level:
kapkan -config /etc/kapkan/config.yaml -log-format json -log-level info
```

The config file is reloadable at runtime without a restart — run [`kapkan -s reload`](#controlling-a-running-daemon),
send `SIGHUP` directly (`systemctl reload kapkan`), or `POST /api/v1/config/reload`. See the
[configuration reference](/docs/configuration) for what reloads and what is fixed at startup.

## Controlling a running daemon

```sh
kapkan -s reload    # re-read the config file (SIGHUP)
kapkan -s stop      # graceful shutdown (SIGTERM)
kapkan -s quit      # alias of stop
```

`-s` reads the process id from `-pid-file` (default `/run/kapkan/kapkan.pid`, written by the
daemon on start) and delivers the matching signal to that process — the same nginx-style local
control as `nginx -s reload`. It needs no network connection and no API token, so it works even
if the REST API is down; it does need permission to signal the daemon, so run it as `root` or the
`kapkan` user.

| `-s` value | Signal | Effect |
| --- | --- | --- |
| `reload` | `SIGHUP` | Hot-reload the config file. Same as `systemctl reload kapkan` or `POST /api/v1/config/reload`. |
| `stop` / `quit` | `SIGTERM` | Graceful shutdown. Same as `systemctl stop kapkan`. The two are aliases — Kapkan's shutdown is always graceful (it asks BGP peers to retain mitigation routes via [Graceful Restart](/docs/mitigation)). |

If the pid file is missing or stale (no such process), or you lack permission to signal it, `-s`
prints the reason to stderr and exits `1`.

<Callout type="info" title="Where the pid file lives">
The bundled [systemd unit](/docs/deployment) sets `RuntimeDirectory=kapkan`, so `/run/kapkan`
exists and is writable by the `kapkan` user; the daemon writes `/run/kapkan/kapkan.pid` there and
removes it on a clean stop. In a local dev run that directory may not exist — the daemon logs a
warning and continues, and `-s` will report "no pid file". Pass `-pid-file /tmp/kapkan.pid` to both
the daemon and `-s` if you want the shortcut locally.
</Callout>

## Utility flags

These flags exit before the daemon starts — they never open a listener or send a route
announcement, so they are safe to run on a production host. (`-check-update` makes a single
outbound request to the releases API; the others are fully offline.)

### Validating a config

```sh
kapkan -check-config /etc/kapkan/config.yaml
```

Runs the engine's exact parse and validation — including cross-field rules a static schema
cannot express — then prints the resolved configuration (mode, protected networks, the
configured flow listeners, the group count, and the effective mitigation per
[hostgroup](/docs/hostgroups)) and exits:

| Exit code | Meaning |
| --- | --- |
| `0` | The config is valid. |
| `1` | The config is invalid; the exact error is printed to stderr. |

The clean 0/1 split drops straight into CI or a pre-deploy gate. See
[Validating the configuration](/docs/configuration#validating-the-configuration).

### Dumping the schema

```sh
kapkan -dump-schema
```

Prints the configuration's JSON schema to stdout and exits `0` (or `1` on an internal error).
The [config builder](/config) consumes this schema to validate fields in your browser.

### Printing the version

```sh
kapkan -version
```

Prints the build version (release tag plus short VCS revision when known) to stdout and exits.
The same string is served at runtime on `/api/v1/status` and carried by the `kapkan_build_info`
[metric](/docs/metrics) — so you can confirm a binary's version locally with zero egress.

### Checking for updates

```sh
kapkan -check-update
```

Runs the [`update_check`](/docs/configuration) once on demand and exits: `0` if up to date, `10`
if a newer release is available, `1` on error. It queries the GitHub Releases API and sends only
the request itself (your IP and a generic User-Agent) — never node identity, config or attack
data. The periodic in-process check is **off by default**; this flag works regardless of that
setting.

## Next steps

- [Quickstart](/docs/quickstart) — download Kapkan and run it in dry-run.
- [Configuration reference](/docs/configuration) — every key in the YAML file.
- [Production deployment](/docs/deployment) — the systemd unit and its flags.
