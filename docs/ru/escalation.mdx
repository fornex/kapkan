export const metadata = {
  title: "Лестницы эскалации",
  description:
    "Усиливайте ответные меры по мере того, как атака затягивается — декларативно, от оповещения к FlowSpec и blackhole.",
};

# Лестницы эскалации

Одиночный метод `mitigation` запускает один и тот же ответ в момент обнаружения атаки.
Лестница эскалации вместо этого **усиливает ответ по мере того, как атака затягивается** —
декларативно, тогда как в FastNetMon вам пришлось бы писать скрипт-обработчик (callback).

```yaml
escalation:                         # supersedes `mitigation` when present
  - { after_seconds: 0,   action: none }       # alert only at first
  - { after_seconds: 30,  action: flowspec }   # still under attack after 30s → surgical drop
  - { after_seconds: 90,  action: divert }     # still under attack after 90s → scrub
  - { after_seconds: 300, action: blackhole }  # still under attack after 300s → blackhole
flowspec:
  action: discard
scrubbing:
  next_hop: "192.0.2.100"   # scrubbing center; see Traffic diversion
  community: "65000:200"
```

[Блок `bgp`](/docs/mitigation#конфигурация-bgp) обязателен, чтобы любая ступень могла
анонсироваться; блоки `flowspec` и `scrubbing` выше нужны только для ступеней `flowspec`
и `divert`.

## Как применяются ступени

Значение `after_seconds` каждой ступени отсчитывается от начала атаки. Ступень применяется,
как только истекло соответствующее время **и бан ещё не был отозван** — отзыв происходит
только после того, как трафик остаётся ниже порога в течение всего окна гистерезиса снятия
бана. Часы отсчёта ступеней при этом непрерывно идут от начала атаки.

- Первая ступень должна быть на `0` секундах.
- `action` — это `none` (только оповещение), `flowspec`, `divert` ([scrubbing](/docs/scrubbing))
  или `blackhole`.
- Лестница может только **удерживать или усиливать** ответ: `none` < `flowspec` < `divert` <
  `blackhole`. Деэскалация между ступенями является ошибкой конфигурации.

## Make-before-break

Переход на новую ступень выполняется по принципу make-before-break (сначала установить,
потом разорвать): новая ступень анонсируется первой, и предыдущая отзывается только после
того, как это удалось, — поэтому жертва ни на мгновение не остаётся без защиты в момент
переключения. Если анонс не удаётся, бан удерживает рабочую ступень и повторяет попытку
на следующем такте. Если сразу подходит срок нескольких ступеней (длительная атака или
демон навёрстывает отставание после паузы), бан сразу прыгает на самую высокую подошедшую
по сроку ступень и никогда не анонсирует пропущенные ступени.

Единственное исключение — переход `divert → blackhole`: оба используют один и тот же
host-route NLRI, поэтому повторный анонс blackhole атомарно заменяет маршрут divert — без
отдельного отзыва, без разрыва. Переключения между семействами (правила FlowSpec на
host-route или обратно) — это описанный выше случай make-before-break.

## Жизненный цикл и совместимость

Лестница **переопределяема для каждой hostgroup** и разделяет остальной жизненный цикл бана:
автоматический отзыв по TTL, ограничение `max_active_bans`, абсолютный whitelist и dry-run
(который продвигает лестницу и логирует каждую ступень, но никогда не анонсирует).

Когда блок `escalation` не задан, одиночный метод `mitigation` ведёт себя ровно как лестница
из одной ступени на `0` секундах — полная обратная совместимость. Каждый бан в `/api/v1/bans`
содержит всю настроенную лестницу целиком (`escalation`), индекс текущей ступени
(`escalation_step`) и метод активной ступени (`method`).

## Связанные разделы

- [Митигация RTBH](/docs/mitigation) — метод blackhole.
- [Митигация FlowSpec](/docs/flowspec) — точечный метод, через который может проходить лестница.
- [Отвод трафика](/docs/scrubbing) — ступень `divert`, которая выполняет scrubbing вместо отбрасывания.
- [Справочник по конфигурации](/docs/configuration) · [REST API](/docs/api)
