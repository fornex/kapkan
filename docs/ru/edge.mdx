export const metadata = {
  title: "Граничные узлы (управляемый HTTPS-фронт)",
  description:
    "Превратите Linux-машину с nginx или Angie в управляемый HTTPS-фронт для ваших зон: сертификаты выпускаются на самом узле, конфигурацию генерирует Kapkan, решения по каждому запросу принимаются локально, а сам Kapkan ничего не пересылает.",
};

# Граничные узлы (управляемый HTTPS-фронт)

<Callout type="info" title="Что это такое — и чем не является">
**Граничный узел** — это Linux-машина с **nginx или Angie**, которой управляет Kapkan: он выпускает
сертификаты зон прямо на машине, генерирует конфигурацию терминатора, по каждому запросу решает,
пропускать ли клиента, и отчитывается мозгу. Kapkan **никогда не пересылает байты клиента** —
TLS терминирует nginx, он же проксирует запросы на ваши исходные серверы (origins); Kapkan лишь
говорит ему, что обслуживать, и для каждого запроса — обслуживать ли его вообще. Сеть (anycast,
транзит, DNS) и машины — ваши; Kapkan приносит контур защиты.
</Callout>

Простыми словами: вы перечисляете свои сайты (зоны) в одном файле на мозге, направляете их DNS на
свои граничные машины, и каждая машина получает сертификат, сгенерированную конфигурацию nginx и
локальную службу решений, которая считает каждого клиента и отказывает тем, кто превышает политику
зоны, — без обращения куда бы то ни было.

```yaml
# zones.yaml on the brain (edge.zones_file) — see the zones reference
zones:
  - name: shop.example.com
    origins: ["10.0.0.10:8080", "10.0.0.11:8080"]
    tls: { min_version: "1.2" }
    policy:
      mode: decide            # every request asks the local decision service
      failure_mode: open      # decider down → requests pass, undecided
      rate: { rps: 50, concurrency: 20 }
```

Эта роль — третья у одного и того же бинарника: `kapkan` (мозг), [`kapkan scrub`](/docs/scrubbing#управляемые-узлы-очистки)
и теперь [`kapkan edge`](/docs/cli#edge). Установка от начала до конца описана в
[руководстве по установке](/docs/edge-install); файл зон — в [справочнике зон](/docs/zones).

## Как это работает

```
 client ──TLS──▶ nginx / Angie  (terminates, proxies to your origins)
                    │ auth_request        │ access log (JSON, unix socket)
                    ▼                     ▼
               kapkan edge  ── decision service · rollups · ACME · renderer
                    ▲ long-poll of the zone document, self-reports
               kapkan (brain) — zones.yaml, issuance coordinator, console
```

- **Мозг распространяет политику, но никогда — вердикты.** `kapkan edge` длинным опросом
  (long-poll) читает один документ — зоны с их исходными серверами, минимальной версией TLS,
  каталогами ACME и политикой по запросам — и хранит копию на диске. Каждое решение по запросу
  принимается **на узле**; упавший или недоступный мозг ничего не меняет в том, как отвечают на
  запросы.
- **Генератор конфигурации** (renderer) превращает документ в конфигурацию nginx в каталоге
  состояния узла: один общий файл (JSON-`log_format`, блоки `upstream` для unix-сокетов Kapkan,
  сервер-заглушка (catch-all), отвергающий неизвестные хосты) и по одному файлу на зону (слушатель
  `:80`, который отвечает на вызовы ACME и выполняет перенаправления, и сервер `:443`, который
  проксирует на исходные серверы за `auth_request` к службе решений). Каждое поколение
  конфигурации проверяется `nginx -t` **до** установки и перезагрузки; кандидат, не прошедший
  проверку, никогда не устанавливается — предыдущая конфигурация продолжает обслуживать запросы, а
  об отказе сообщается.
- **Служба решений** отвечает на `auth_request` nginx через unix-сокет за микросекунды:
  `200` (с необязательным заголовком `X-Kapkan-Mark`, который получает исходный сервер) или `403`.
  Она применяет `policy.rate` зоны для каждого **источника** — IPv4-адреса или IPv6-подсети
  `/64` — с token bucket для запросов в секунду и приближённым подсчётом одновременных запросов
  для параллелизма, и держит ограниченную таблицу вердиктов (запрет или метка, с TTL). Клиент,
  превысивший потолок, получает `429` с `Retry-After`; клиент, которого отвергает таблица, — `403`.
- **Свёртки** (rollups) читают лог доступа терминатора (JSON через unix-датаграммный сокет) в окна
  по зонам и источникам. Источник, который продолжает ломиться через потолок частоты, переводится
  в запрет (на минуту, при повторе — удваивается до десяти); источник, чьи запросы почти сплошь
  ошибки, получает метку `errors`, видимую исходному серверу. Вердикты остаются на узле.
- **ACME** работает на каждом узле: ключи аккаунта и сертификаты генерируются на машине и никогда
  её не покидают. Мозг лишь упорядочивает выпуск по зонам (в один момент заказывает один узел) и
  рассылает ожидающий вызов HTTP-01 на все узлы, так что проверка со стороны CA может прийти на
  любой из них. И то и другое вспомогательно: узел продлевает сертификат и без мозга.

### Разделение на быстрый и медленный пути

Изменение **частоты** зоны или **вердикта** никогда не перезагружает nginx: они попадают в службу
решений и вступают в силу на следующем запросе. Только **медленные** изменения — добавленная или
удалённая зона, изменённый исходный сервер, минимальная версия TLS, выпущенный или продлённый
сертификат — порождают новое поколение конфигурации, которое проверяется и перезагружается. Именно
это позволяет ужесточить частоту под атакой, не трогая терминатор.

### Dry-run: сначала наблюдайте

Как и всякая удалённая роль, граничный узел **по умолчанию работает в режиме dry-run**: решения
подсчитываются и помечаются, но ни одно не применяется. Запрос, который политика отвергла бы,
всё равно доходит до исходного сервера — с заголовком `X-Kapkan-Mark: would-deny:rate` (или
`would-deny:concurrency`, `would-deny:table:<reason>` — сегодня `would-deny:table:flood`, правило
свёртки, которое перевело источник в таблицу), так что вы можете поискать его в логах исходного
сервера и посмотреть метрики до того, как будет отвергнут хоть один клиент. Когда подсчёты выглядят
верно, задайте `dry_run: false` в `edge.yaml` узла.

## Что делает узел при отказах

| Отказ | Поведение |
| --- | --- |
| Мозг упал или недоступен | Узел продолжает обслуживать последний документ и сертификаты с диска; продления продолжаются; после перезагрузки узел возвращается в строй без мозга (fail-static: работа на последнем известном состоянии). За общим или anycast-адресом проверка HTTP-01 при продлении может прийти на другой узел, пока мозг недоступен; такие попытки повторяются ежечасно и никогда не засчитываются в пользу запасного CA. |
| Служба решений упала или отвечает медленно | Для каждой зоны: `failure_mode: open` (по умолчанию) пропускает запрос без решения и учитывает его; `closed` отвечает `503`. |
| Документ отклонён (генератором конфигурации или `nginx -t`) | Предыдущее поколение продолжает обслуживать запросы; узел остаётся здоровым, но с `converged: false`, сообщает ETag **обработанного** документа (а не принятого) и повторяет попытку локально каждые 1 → 10 минут, пока документ не применится или не придёт более новый. |
| nginx умер | systemd перезапускает его; при заданном `terminator.pid_file` узел тем временем сообщает `alive: false` и отвечает `503` на своём `/healthz`. Kapkan никогда не надзирает за терминатором сам. |
| Сертификат не удаётся продлить | Текущий сертификат обслуживает до истечения срока; `kapkan_edge_cert_not_after_seconds` — сигнал тревоги за 30 дней до истечения (T−30 д); после трёх неудач узел пробует запасной CA. |
| Узел умер | Мозг видит, что опросы прекратились (`edge.stale_after_seconds`), и показывает это в инвентаре; увести трафик — задача вашей сети (отзыв anycast, DNS). |

## Как читать состояние узла

- **`/healthz`** на `status_listen` отвечает `200`, пока действует проверенное поколение
  конфигурации, созданное самим узлом (и терминатор жив, когда настроен pid-файл), иначе `503`.
  В теле — `converged`, ETag обработанного и принятого документа, действующее поколение и результат
  его проверки, последняя ошибка, время следующей попытки, когда мозг видели в последний раз, и
  `dry_run`.
- **`/metrics`** по тому же адресу отдаёт серии Prometheus узла — см.
  [метрики](/docs/metrics#граничные-узлы).
- **Инвентарь мозга**, `GET /api/v1/edge/nodes`, объединяет живость каждого настроенного узла с его
  последним самоотчётом: версия, dry-run, ETag обработанного документа, тип и версия терминатора,
  поколение и результат проверки, сертификаты (но никогда — ключ). См. [API](/docs/api#канал-граничных-узлов).

## Ограничения, о которых стоит знать

- **Минимальная версия TLS на старом nginx.** nginx до 1.29.2 применяет `ssl_protocols` сервера
  *по умолчанию* ко всем именам, поэтому заглушка узла несёт **наименьший** `tls.min_version` из
  всех его зон, и каждая зона получает именно этот минимум. Angie и nginx ≥ 1.29.2 соблюдают
  минимум для каждой зоны отдельно. Где это важно, держите все зоны на одном минимуме или
  используйте Angie.
- **Один сервер по умолчанию.** Kapkan генерирует собственную заглушку `default_server` на `:80` и
  `:443`, которая отвергает неизвестные хосты и SNI. Если ваш `nginx.conf` уже объявляет такой
  (сайт по умолчанию в Debian объявляет), `nginx -t` падает с ошибкой *a duplicate default server* —
  задайте `omit_catch_all: true` и оставьте эту роль своим серверам.
- **Исходные серверы разрешаются при проверке.** Исходный сервер, заданный именем хоста,
  разрешается один раз — когда nginx загружает конфигурацию; имя, которое не разрешается, роняет
  `nginx -t` для всего поколения (и попытка повторяется). Предпочитайте адреса.
- **Подстановочные (wildcard) имена, HTTP/3 и проверки клиентов — следующие этапы.** `tls.h3` и
  `policy.challenge`, отличный от `off`, сегодня отклоняются; каждое имя перечисляется явно.
- **Лимиты на дублирующие сертификаты.** Сертификат на каждом узле означает, что N узлов на одной
  зоне — это N заказов; Let's Encrypt ограничивает одно имя примерно пятью в неделю. Слот выпуска
  на мозге и случайный разброс времени продления разносят их; `acme.fallback` (ZeroSSL, Google
  Trust Services — обоим нужна привязка внешнего аккаунта, External Account Binding) — запасной
  выход. Для больших парков планируйте аккаунты CA заранее.
- **Токен агента выпускает сертификаты.** Пока токены не привязаны к узлам, владелец токена
  `agent` может опубликовать через мозг ответ на вызов для любой зоны (только с узла, который держит
  слот выпуска этой зоны, и каждый вызов записывается в журнал). Если узел скомпрометирован,
  смените токен.

## Связанные разделы

- [Установка граничного узла](/docs/edge-install) — сторона мозга, сторона nginx, юнит, боевой режим
- [Справочник зон](/docs/zones) — каждый ключ `zones.yaml`
- [`kapkan edge`](/docs/cli#edge) — команда, её флаги и `edge.yaml`
- [Канал граничных узлов](/docs/api#канал-граничных-узлов) — маршруты мозга
- [Метрики](/docs/metrics#граничные-узлы) — серии `kapkan_edge_*`
- [Плоскость отпечатков](/docs/fingerprinting) — блокировка клиентов по JA4 вне пути обработки, до терминатора
