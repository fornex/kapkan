export const metadata = {
  title: "Быстрый старт",
  description:
    "Загрузите готовый release Kapkan, запустите его в режиме dry-run со встроенным примером конфигурации и понаблюдайте за первым обнаружением атаки.",
};

# Быстрый старт

Это руководство проведёт вас от пустого места до работающего экземпляра Kapkan в режиме dry-run,
который обнаруживает атаки на тестовый префикс. Шага сборки нет — Kapkan поставляется как единый
готовый подписанный бинарник, — и ничто из описанного здесь не способно анонсировать маршрут,
потому что режим dry-run включён по умолчанию.

<Callout type="info" title="Что вам нужно">
Хост на Linux (`amd64` или `arm64`). Чтобы видеть обнаружения *в реальном времени*, вам также
понадобится маршрутизатор, экспортирующий NetFlow / IPFIX / sFlow, — но даже без него вы можете
проверить сквозное обнаружение с помощью встроенного инструментария синтетических атак
([последний раздел](#5-нет-маршрутизатора-сгенерируйте-синтетические-атаки), который запускается из клона
исходного кода). Go-инструментарий нужен только если вы решите
[собрать из исходного кода](/docs/deployment#building-from-source) вместо загрузки.
</Callout>

## 1. Установка Kapkan

Выберите тот вариант, который подходит вашему хосту, — каждый release поставляется готовым и
подписанным для `linux` `amd64`/`arm64`, поэтому вам ничего не нужно компилировать.

### Debian / Ubuntu (`.deb`)

```sh
VER=v1.0.0
curl -fLO "https://github.com/fornex/kapkan/releases/download/$VER/kapkan_${VER#v}_linux_amd64.deb"
sudo apt install "./kapkan_${VER#v}_linux_amd64.deb"
```

Пакет устанавливает бинарник в `/usr/local/bin/kapkan`, создаёт непривилегированного пользователя
`kapkan`, формирует структуру `/etc/kapkan` с конфигурацией `config.yaml` в режиме dry-run и
устанавливает защищённый юнит systemd — оставленный **остановленным**, чтобы вы могли сначала
просмотреть конфигурацию. Страница [Развёртывание в продакшене](/docs/deployment) описывает работу
с сервисом дальше. На RHEL / Fedora всё то же самое с `.rpm`:
`sudo dnf install ./kapkan_<ver>_linux_amd64.rpm`.

### Tarball (любой дистрибутив)

```sh
VER=v1.0.0
curl -fLO "https://github.com/fornex/kapkan/releases/download/$VER/kapkan_${VER#v}_linux_amd64.tar.gz"
tar xzf "kapkan_${VER#v}_linux_amd64.tar.gz"
```

Это распаковывает бинарник `kapkan` плюс каталог `deploy/` (юнит systemd, пример конфигурации и
`update.sh`). Остальная часть этой страницы использует tarball. Прежде чем запускать его в
продакшене, стоит [проверить загрузку](/docs/upgrading#проверка-загрузки-вручную) — подпись
cosign и SHA-256.

<Callout type="info" title="Используйте последний release">
Замените `v1.0.0` на новейший тег со [страницы релизов](https://github.com/fornex/kapkan/releases/latest),
а `amd64` — на `arm64` на хостах ARM. Имена архивов и пакетов опускают ведущую `v`
(например, `kapkan_1.0.0_linux_amd64.tar.gz`).
</Callout>

## 2. Запуск в режиме dry-run

Из распакованного tarball запустите Kapkan со встроенным примером конфигурации и логами в читаемом
виде:

```sh
./kapkan -config deploy/config.example.yaml -log-format text
```

`-config` указывает на YAML-конфигурацию, а `-log-format text` переключает логи с используемого по
умолчанию структурированного JSON на читаемый человеком вид — полный список флагов и допустимые для
них значения см. в [Справочнике CLI](/docs/cli).

Встроенный [`deploy/config.example.yaml`](/docs/configuration) содержит `dry_run: true`, тестовые
защищаемые сети `203.0.113.0/24` и `2001:db8::/32`, `protected_whitelist` и слушает sFlow на
`:6343`, а NetFlow/IPFIX — на `:2055`. Поскольку режим dry-run включён, Kapkan записывает
потенциальные blackhole-маршрутизации, но никогда не анонсирует маршрут.

<Callout type="info" title="Установили пакет?">
Если вы установили `.deb`/`.rpm`, тот же демон вместо этого работает как сервис с
`/etc/kapkan/config.yaml` — `sudo systemctl start kapkan`, затем `journalctl -u kapkan -f`. См.
[Развёртывание в продакшене](/docs/deployment).
</Callout>

## 3. Направьте экспортёры на Kapkan

Настройте flow-экспортёры ваших маршрутизаторов на отправку данных на порты, которые слушает Kapkan:

| Протокол | Порт по умолчанию |
| --- | --- |
| sFlow v5 | `:6343` |
| NetFlow v5/v9 + IPFIX | `:2055` |

NetFlow и IPFIX используют один и тот же UDP-сокет. Должен быть настроен хотя бы один слушатель.

## 4. Понаблюдайте за работой

По умолчанию Kapkan отдаёт свой REST API и метрики на `127.0.0.1:8080`:

```sh
# Current mode, protected networks, thresholds, active attack/ban counts
curl -s localhost:8080/api/v1/status | jq

# Active attacks plus the last 100 that ended, with samples and classification
curl -s localhost:8080/api/v1/attacks | jq

# Prometheus metrics
curl -s localhost:8080/metrics | grep kapkan_
```

Также можно открыть встроенную [панель управления](/docs/dashboard) по адресу
[http://localhost:8080](http://localhost:8080) в браузере.

## 5. Нет маршрутизатора? Сгенерируйте синтетические атаки

Этот последний шаг запускается из **клона исходного кода** (он задействует набор тестов), поэтому
ему нужны репозиторий и Go-инструментарий. В состав Kapkan входит `pkg/flowgen`, который формирует
реальные датаграммы NetFlow / sFlow в проводном формате — включая паттерны amplification,
SYN-flood и UDP-flood. Он используется на протяжении всего набора тестов для проверки сквозного
обнаружения без обращения к реальному маршрутизатору. Интеграционный тест в `internal/app`
воспроизводит флуд типа NTP-amplification через реальный UDP-сокет, направляя его на экземпляр в
режиме dry-run, и убеждается, что атака и её (автоматически истекающая) виртуальная блокировка —
записанная потенциальная blackhole-маршрутизация, которую dry-run отдаёт через API, но никогда не
анонсирует, — появляются в API:

```sh
git clone https://github.com/fornex/kapkan && cd kapkan/engine
go test ./internal/app -run TestEndToEnd -v
```

## Дальнейшие шаги

- [Развёртывание в продакшене](/docs/deployment) — запустите Kapkan как защищённый сервис systemd.
- [Справочник по конфигурации](/docs/configuration) — адаптируйте конфигурацию под свою сеть.
- [Обнаружение и пороги](/docs/detection) — настройте, что считается атакой.
- [Переход в боевой режим](/docs/going-live) — проверьте всё, затем отключите dry-run.
