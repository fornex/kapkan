export const metadata = {
  title: "Хранилище (ClickHouse)",
  description:
    "Опциональное сохранение истории атак и трафика в ClickHouse — для расследований и графиков на дашбордах; никогда не блокирует детектирование.",
};

# Хранилище (ClickHouse)

Укажите Kapkan на сервер ClickHouse, чтобы хранить историю атак и трафика — ответ на вопрос
«что нас атаковало в прошлый вторник». Без этого Kapkan работает полностью в памяти процесса
на «живых» данных.

<Callout type="info" title="По принципу best-effort, никогда не блокирует детектирование">
Сохранение работает через ограниченную очередь с неблокирующей отправкой. Медленный или
недоступный ClickHouse приводит к отбрасыванию строк (учитываются в `storage_rows_total` со
значением `result="dropped"`), а не к задержке горячего пути движка.
</Callout>

## Включение

```yaml
storage:
  clickhouse:
    url: "http://127.0.0.1:8123"   # empty/absent disables persistence
    database: "kapkan"             # created if absent
    username_env: "KAPKAN_CH_USER" # optional; credentials come from the env
    password_env: "KAPKAN_CH_PASS"
    ttl_days: 7                    # rows auto-expire (per-row TTL)
    flush_interval_seconds: 5
    batch_size: 1000
    queue_size: 100000
    traffic_interval_seconds: 10
```

Учётные данные читаются из указанных переменных окружения (`username_env` / `password_env`),
а не из конфигурационного файла.

## Как это работает

Kapkan обращается к **HTTP-интерфейсу** ClickHouse средствами стандартной библиотеки Go — без
зависимости от драйвера. Единственная внешняя зависимость — сам сервер ClickHouse.

При запуске он идемпотентно создаёт две таблицы MergeTree:

| Таблица | Содержимое |
| --- | --- |
| `attack_events` | Каждое начало и окончание атаки: тип, направление, скорости, топ источников из выборки и состояние блокировки. |
| `traffic` | Периодические снимки скорости и базовой линии (baseline) по каждому хосту (каждые `traffic_interval_seconds`). |

Обе таблицы используют построчный TTL `ttl_days`, поэтому срок хранения ограничен без
вмешательства оператора.

## Обратное давление (backpressure)

Строки помещаются в ограниченную очередь (`queue_size`) с неблокирующей отправкой и
сбрасываются пакетами (`batch_size` / `flush_interval_seconds`). Если ClickHouse работает
медленно или недоступен, строки отбрасываются — учитываются в `storage_rows_total` со
значением `result="dropped"` — а движок продолжает работу. См. [Метрики](/docs/metrics).

## Что сохраняется, а что нет

Таблица `traffic` сейчас сохраняет только снимки по каждому хосту. Агрегация по ASN не
сохраняется (Kapkan не определяет ASN из потоков flow), а итоги по hostgroup пока не
снимаются — оба варианта рассматриваются для последующей доработки.

## Связанные разделы

- [Справочник по конфигурации](/docs/configuration) — полный список ключей `storage`.
- [Метрики](/docs/metrics) — счётчик `storage_rows_total`.
