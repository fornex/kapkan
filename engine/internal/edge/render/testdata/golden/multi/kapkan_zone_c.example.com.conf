# Zone c.example.com — generated by kapkan edge, DO NOT EDIT (see kapkan_00_common.conf).
#
# NO CERTIFICATE YET: only the plain-HTTP listener is rendered, and it serves
# nothing but ACME challenges — kapkan never proxies a zone over cleartext.
# The TLS server appears on the first render after issuance (edge-spec §3).

upstream kapkan_origin_c_example_com_6e5b26 {
    server 10.0.3.1:443;
    keepalive 32;
}

server {
    listen 80;
    server_name c.example.com;
    access_log syslog:server=unix:/run/kapkan-test/log.sock,nohostname,tag=kapkan kapkan_edge;

    location ^~ /.well-known/acme-challenge/ {
        proxy_pass http://kapkan_challenge;
        proxy_set_header Host $host;
        proxy_set_header X-Kapkan-Zone c.example.com;
    }

    location / {
        return 503;
    }
}
